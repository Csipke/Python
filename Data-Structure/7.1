# Write a program that prompts for a file name, then opens that file and reads through the file, and print the contents of the file in upper case.
# Use the file words.txt to produce the output below. You can download the sample data at http://www.pythonlearn.com/code/words.txt 

fname = raw_input("Enter file name: ") # fájlnév bekérése 
fh = open(fname, 'r')                  # fájl megnyitása olvasásra 
for line in fh:                        # mit tegyen: 
    if line == "\n":                   # ha a sor = üres sor (csak egy sorvég) 
            pass                       # akkor menjen tovább (hagyja ki)
        print line.strip("\n").upper() # egyébként meg "vágja szalagokra" a sorokat a sorvégeknél, változtassa nagybetűssé és írja ki

# lehet finomítani a programon: ha nem jó a fájlnév, azt is írja ki (try: except használható)
