# 6.5 Write code using find() and string slicing (see section 6.10) 
# to extract the number at the end of the line below.
# Convert the extracted value to a floating point number and print it out.


text = "X-DSPAM-Confidence:    0.8475";    # ebben a szövegben keressük meg a számot (szöveg formátumú!)
atpos = text.find('0')                     # startpozíció: 0
sppos = text.find('5')                     # végpozíció 5
num1 = text[atpos:sppos+1]                 # a szövegben jelöljük ezt a karakterláncot (+1, mert ADDIG tart csak, de már nem számít bele)
num2 = float(num1)                         # tizedessé alakítjuk
print num2 # kiíratjuk
